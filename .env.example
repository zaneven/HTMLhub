# Worker API URL
# 留空（默认）：纯静态模式，无需后端，直接读取本地 file-index.json
# 填写后：自动启用云端模式，支持在线登录、多文件上传、增删管理
VITE_API_URL=

# 示例（部署Cloudflare Workers 后的地址）：
# VITE_API_URL=https://htmlhub-api.your-subdomain.workers.dev