# Worker API URL (云端模式)
# 配置此变量后启用云端模式，支持登录、上传、管理功能
# 留空则使用静态模式，从本地 file-index.json 读取数据
VITE_API_URL=

# 示例：部署到 Cloudflare Workers 后的 URL
# VITE_API_URL=https://htmlviewer-api.your-subdomain.workers.dev
